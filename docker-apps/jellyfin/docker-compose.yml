services:
    server:
        image: 'vaultwarden/server:latest'
        ports:
            - '6821:80'
        restart: always
        environment:
            - 'ADMIN_TOKEN=???'
        volumes:
            - '/etc/localtime:/etc/localtime'
            - '???:/data/'
        container_name: vaultwarden
